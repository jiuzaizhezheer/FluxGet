# 前端开发要求

每次进行任何前端相关工作之前，必须先完整阅读并遵循项目根目录下的 [`CREATIVE_MODE_PROMPT.md`](./CREATIVE_MODE_PROMPT.md)。

前端相关工作包括但不限于：

- 页面、组件和布局的新增或修改；
- 颜色、字体、间距、边框、阴影和动画的调整；
- 亮色模式、暗色模式和响应式样式的修改；
- 交互状态、视觉反馈和可访问性样式的实现；
- 根据设计稿、截图或其他参考资料进行界面改造。

如果用户的当前明确要求与 `CREATIVE_MODE_PROMPT.md` 冲突，以用户当前要求为准；除此之外，不得自行偏离其中定义的颜色令牌、字体系统、组件规则、构图约束和验收标准。

完成前端修改后，必须按照 `CREATIVE_MODE_PROMPT.md` 中的验收清单进行检查，并运行 TypeScript 检查和生产构建。不得以视觉改造为由修改无关的路由、API、状态管理或业务逻辑。
