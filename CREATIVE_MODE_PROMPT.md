# Creative Mode 前端视觉规范

> 本文件是 FluxGet 前端在样式、颜色和视觉风格方面的长期约束规范，不是用于从零生成页面的一次性提示词。任何前端新增、修改、重构或视觉修复都必须以本文件为基准；没有涉及视觉调整的业务代码不受本规范干预。

## 规范优先级

遇到规则冲突时，严格按以下顺序执行：

1. 不可变的产品行为与可访问性要求。
2. 本文中的“严格禁止”和“验收清单”。
3. 原始 HTML 的 `Frame Rules`。
4. 固定设计令牌、字体和组件规则。
5. 示例构图和装饰细节。

原始 Cover 示例同时出现了 Pink、Green、Orange、Yellow 四种强调色，但其 `Frame Rules` 明确规定每个画面只能使用 2–3 种强调色。这里以 `Frame Rules` 为准：Cover 仅作为构图参考，四种强调色同时出现视为展示例外，不复制到产品页面。

## 适用范围与核心定位

FluxGet 的所有前端界面必须遵守大胆、编辑感强的新粗野主义（Neo-Brutalist）视觉语言。整体风格应体现独立杂志、印刷海报与创意工作室工具的结合：直接、有冲击力、结构清晰，同时始终服务于真实产品功能，不能为了追求形式而降低可用性。

本规范适用于：

- 页面背景、文字、边框、阴影、图标和装饰元素的颜色选择；
- 页面、组件、导航、表单、卡片、表格和状态反馈的视觉样式；
- 字体角色、字号层级、间距、构图、响应式与暗色模式；
- hover、focus-visible、disabled、error、success 等交互状态；
- 新页面和新组件的视觉设计，以及既有界面的样式调整与重构。

本规范不要求为了统一风格而重写组件逻辑，也不得被用作修改路由、API、状态管理或业务行为的理由。

### 视觉基调

- 使用温暖的奶油色作为通用画布，绝不使用纯白背景。
- 使用近黑色承担正文、标题、边框、分隔线和界面框架。
- 采用大色块、粗边框、硬边阴影、醒目的几何形状和清楚的栅格结构。
- 整体保持扁平、锐利、印刷感；不要玻璃拟态、柔和渐变、发光或模糊阴影。
- 每个主要区域只表达一个核心信息，保持强视觉层级和充足留白。
- 每个主要区域最多使用 2–3 个强调色，不要在同一区域同时堆叠全部强调色；Canvas、Ink 与 Cream 2 属于基础色，不计入强调色数量。

### 固定颜色令牌

- Canvas / Cream：`#EFE9D9`，页面主背景，温暖但不发白。
- Recessed / Cream 2：`#E4DCC4`，输入框、次级面板、表格内容区等下沉表面。
- Ink：`#0F0F0F`，标题、正文、粗边框、规则线和主要界面框架。
- Ink 2：`#2A2A2A`，次级正文和弱化信息。
- Green：`#1F8A4C`，大字号展示区域、成功状态或收尾色块；Cream/Green 仅用于大号文字。
- Green Dark：`#136636`，普通字号主按钮和状态卡背景，确保 Cream 文字具有足够对比度。
- Pink：`#F06CA8`，品牌高能强调、标记、徽章和重点词。
- Pink Dark：`#D14E8B`，粉色组件的深色层次。
- Orange：`#E85A1F`，硬阴影、警示和高冲击数据色块；普通字号文字使用 Ink，不使用 Cream。
- Yellow：`#F5C518`，小面积暖色标点、标签、徽章和几何装饰。

不要擅自增加第五种强调色。颜色应是纯色块，不使用渐变。

### 字体系统

- 展示标题使用 `Archivo Black`，仅用于短标题、数字、品牌字标和声明式文案。
- 展示标题使用全大写风格；建议行高 `0.86–0.92`、字距 `-0.01em` 至 `-0.02em`。
- 正文使用 `Space Grotesk`，保持清晰、现代、略带几何感；原始规范要求正文左对齐，行高约 `1.4–1.6`。
- 标签、序号、技术信息和元数据使用 `JetBrains Mono`，全大写并增加 `0.06em–0.14em` 字距。
- 中文标题使用厚重的无衬线黑体作为回退字体，不要为了模仿拉丁字体而损害中文可读性。
- 不要将 Archivo Black 用于长正文或句子式大小写文本。

### 构图与布局

- 使用明确的网格、大留白和粗横向分隔线建立编辑式节奏。
- 标题尺寸可以大胆，但必须按内容自然换行，不要为了填满宽度而把文字撑到页面边缘。
- 顶部区域可由品牌字标、胶囊标签、短导航和状态信息组成。
- 主要行动区域放在视觉中心或明显的左侧主轴上。
- 装饰图形使用简单圆形、旋转方块、印章或实心色块；它们不能遮挡内容或影响操作。
- 响应式布局中保留粗边框、纯色和强层级，但缩小装饰物与硬阴影距离，避免移动端拥挤。

### 原始几何与间距令牌

以下数值来自参考 HTML。展示型页面优先使用原始数值；FluxGet 这类工具型产品界面按照后文的产品化规则缩放：

- 文档区块：`padding: 120px 96px`，区块之间使用 `4px` Ink 横向分隔线。
- Cover：最小高度 `100vh`，内边距 `64px`，纵向间距 `40px`。
- Cover 主标题：`clamp(72px, 13vw, 210px)`，行高 `0.86`，字距 `-0.02em`。
- 中型展示标题：约 `52–96px`，行高 `0.92`。
- 正文大号规格：约 `24–28px`，行高 `1.4`。
- 组件网格：12 列，列间距 `28px`。
- 色板网格：5 列，间距 `24px`。
- 数据卡片与步骤卡片：`4px` Ink 边框；步骤卡片参考高度 `300px`。
- 表格外边框 `4px`，单元格内部边框 `3px`。
- 普通胶囊边框 `2px`；除胶囊外不使用圆角。
- Featured Marker：`24px 24px 0` Orange 硬阴影，并增加 `4px` Ink 阴影外框。
- Cover Badge：约 `260px × 260px`，旋转 `-6deg`，`4px` Ink 边框。
- 大型装饰圆：约 `300px × 300px`，`4px` Ink 边框。
- 原始响应式断点为 `1100px`：双列画廊变单列，色板由 5 列缩为 2 列。

### 原始构图模板

- **Wordmark Cover**：品牌字标居中，顶部与底部使用等宽元信息，重点词使用单一强调色。
- **Big Claim**：左对齐超大声明，一个画面只表达一个观点，以纯色背景承载。
- **Stat Grid**：标题居中，下方放置三列数据卡片；数据数字使用展示字体，标签使用等宽字体。
- **Closing Plate**：Green 满版背景、Cream 文字、居中声明，可辅以旋转 Pink 印章。
- **Section Header**：索引标签 + 展示标题 + 4px 横向规则线，三者基线对齐。

### 组件语言与尺寸层级

- 展示卡片、主要行动容器和区块分隔使用 `4px` Ink 实线边框。
- 表格单元格和中等强调容器使用 `3px` Ink 实线边框。
- 输入框、按钮、导航和胶囊使用 `2px` Ink 实线边框。
- 默认不使用圆角；只有胶囊标签等明确需要表达“胶囊”的组件可以使用完全圆角。
- 展示型 Featured Marker 使用 `24px` 硬偏移阴影；产品主行动区使用 `10–12px`；紧凑控件和卡片使用 `3–8px`。所有阴影的模糊半径必须为 `0`。
- 普通内容卡片不要全部加重阴影，仅给主行动区、重点标记或关键反馈使用硬阴影。
- 展示型主色块可使用 Green 与 Cream 大号文字；产品主按钮使用 Green Dark 底色与 Cream 文字，悬停时可切换为 Pink 底色与 Ink 文字。
- 次级按钮使用 Cream 底色、Ink 文字和 Ink 粗边框；悬停时用 Yellow 或 Pink 填充。
- 输入框使用 Cream 2 底色和 Ink 粗边框；聚焦态使用 Pink 内描边或清晰的硬轮廓。
- 数据卡片可按 Green、Pink、Orange、Cream 轮换，但单个区域仍应遵守最多 2–3 个强调色的限制。
- 错误或警告提示使用 Orange 与 Ink 普通文字；成功展示区使用 Green，承载普通文字的成功状态使用 Green Dark。
- 表格标题行使用 Ink 底色与 Cream 文字，内容行使用 Cream 2，并保留清晰的网格边框。

### 组件状态矩阵

| 组件 | Default | Hover | Focus visible | Disabled |
| --- | --- | --- | --- | --- |
| 主按钮 | Green Dark / Cream，2px Ink 边框 | Pink / Ink | 3px Pink 外轮廓，3px offset | 40% opacity、禁止光标，不触发 hover 换色 |
| 次级按钮 | Canvas / Text，2px Line 边框 | Yellow 或 Pink / Ink | 3px Pink 外轮廓 | 降低不透明度并保留标签可读性 |
| 文本输入框 | Cream 2 / Ink，2px Ink 边框 | 不依赖 hover | 2px Pink 内描边或等效高对比轮廓 | 使用弱化表面与明确禁用光标 |
| 导航链接 | Canvas / Text，2px Line 边框 | 单一强调色填充 | 3px Pink 外轮廓 | 如存在禁用态，应不可激活 |
| 错误提示 | Orange / Ink，4px Line 边框 | 无 | 不可交互 | 不适用 |

### 交互与可用性

- 所有按钮、链接和输入框必须具有清楚的 hover、focus-visible、disabled 状态。
- 焦点轮廓必须高对比且不可仅依赖颜色细微变化。
- 不要为了视觉风格删除语义标签、键盘导航、ARIA 属性或响应式行为。
- 视觉装饰必须设置为不可交互，不能覆盖点击区域。
- 保持正文对比度和信息可读性；高饱和强调色只用于短文本、状态或局部色块。

## FluxGet 产品化适配

以下规则不是原始 HTML 的固有内容，而是将其安全地用于 FluxGet 工具界面时的新增约束：

- 桌面端内容宽度限制在约 `1152px`，页面水平留白采用 `24px`，主内容顶部与底部采用约 `64px`。
- 首页采用 **Big Claim** 构图，标题和说明文字左对齐；下载表单是唯一 Featured Action。
- 环境页采用 **Section Header + Stat Grid** 构图，状态卡片最多轮换 Pink、Yellow、Green Dark 三种强调色。
- 原始 `24px` Featured Marker 阴影缩为表单 `10px`、状态卡片 `7px`、主题按钮 `3px`，避免工具界面显得笨重。
- 中文展示标题使用系统无衬线黑体回退；英文品牌、数字和短标签继续使用指定字体。
- 不改变既有路由、API、React 状态、表单事件、主题持久化、ARIA 与响应式行为。

### 暗色模式适配

- 将 Canvas 切换为 Ink `#0F0F0F`，将主要文字和边框切换为 Cream `#EFE9D9`。
- 将次级表面切换为 Ink 2 `#2A2A2A`，次级文字切换为 Cream 2 `#E4DCC4`。
- Pink、Green、Orange、Yellow 保持原始色值，以维持品牌识别。
- 暗色模式仍使用纯色、粗边框和硬阴影，不引入发光、透明玻璃或灰色渐变。

### 前端实现约束

- 先读取现有项目结构与行为代码，再修改样式；不能用重写页面的方式绕过既有逻辑。
- 颜色必须来自本文令牌，不新增随意色值；透明度变化不视为新颜色，但不能造成低对比度。
- 优先复用 CSS 变量管理设计令牌，组件层使用清晰、可追踪的样式类。
- 不新增与视觉目标无关的依赖；字体资源属于视觉依赖，必须提供系统字体回退。
- 装饰元素必须 `pointer-events: none`，需要时增加 `aria-hidden`，不能进入键盘焦点顺序。
- 修改完成后必须说明改动文件，并运行 TypeScript 检查与生产构建。

### 严格禁止

- 不使用纯白背景。
- 不使用渐变。
- 不使用模糊阴影、柔光或霓虹光晕。
- 不滥用圆角；除胶囊组件外尽量保持直角。
- 不在同一画面使用全部四种强调色。
- 不增加未经定义的新强调色。
- 不让装饰元素抢占功能内容或降低可访问性。
- 不把所有组件都做成同等强烈；必须有主次层级。

所有最终视觉呈现都必须保持“奶油纸张 + 黑色油墨 + 高饱和专色印刷”的统一感受：标题大胆、边界明确、色块直接、阴影坚硬，既保留创意海报气质，又维持现代工具界面的清晰与可操作性。

## 验收清单

- [ ] 页面背景没有纯白，主画布使用 Cream；暗色模式使用 Ink。
- [ ] 没有渐变、backdrop blur、发光或模糊阴影。
- [ ] 除胶囊标签和圆形装饰外，没有圆角组件。
- [ ] 同一主要区域最多出现 3 种强调色。
- [ ] 展示容器为 4px、中等容器为 3px、控件为 2px 边框。
- [ ] 所有阴影均为零模糊硬阴影，并符合 24 / 10–12 / 3–8px 层级。
- [ ] 展示标题、正文、等宽标签各自使用正确字体角色。
- [ ] 正文默认左对齐；只有 Wordmark、Stat 标题和 Closing Plate 等指定构图可居中。
- [ ] 按钮、输入框和导航具有清晰的 hover、focus-visible、disabled 状态。
- [ ] 普通文本与背景的对比度不低于 `4.5:1`；大号文字不低于 `3:1`。
- [ ] 装饰元素不遮挡内容、不响应点击、不进入可访问性树或焦点顺序。
- [ ] 移动端没有横向溢出，硬阴影和装饰元素已适当缩小或隐藏。
- [ ] 原有路由、API、状态管理、事件处理和主题切换逻辑未改变。
- [ ] TypeScript 检查和生产构建通过。

## 核心遵守声明

FluxGet 的全部前端样式、颜色和视觉表达必须遵守本规范：以 `#EFE9D9` 奶油色和 `#0F0F0F` 墨黑为基础，有限使用 `#F06CA8` 粉色、`#1F8A4C` 绿色、`#E85A1F` 橙色与 `#F5C518` 黄色；按照 Archivo Black、Space Grotesk、JetBrains Mono 的字体角色，以及 4px / 3px / 2px 边框和 24px / 10–12px / 3–8px 零模糊硬阴影层级实施；保持直角组件、大字排版和编辑式栅格，禁止纯白、渐变、玻璃拟态、柔光与圆角泛滥，并且不得破坏响应式、可访问性或任何既有产品行为。
